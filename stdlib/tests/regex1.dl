
expected-results 5.

query "Regex1" has result "passed" if
    "a+b+c+" has regex-match "aaabbc".

query "Regex 2" has group n, text t if
    "(a+)(b+)(c+)" has regex-match "aaabbc", group n, value t.
