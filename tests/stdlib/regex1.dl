
expected-results 5.

query "Regex1" has result "passed" if
    regex "a+b+c+" has match "aaabbc".

query "Regex 2" has group n, text t if
    regex "(a+)(b+)(c+)" has match "aaabbc", group n, value t.
