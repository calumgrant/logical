expected-errors 3.

foo X.
foo _.
_ has cash _.

foo X has bar Y.  // OK - treated as strings.
foo @X has bar Y.

f X if 1=1.  // Error

f Y if 1=1+Y.  // Error

X is rich if find sum C, V in (X has cash C and C has value V) > 100.
rich _.

select r if rich r.
