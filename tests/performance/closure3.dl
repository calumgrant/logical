expected-results 6.
// evaluation-step-limit 21272.

x is a number if 0<=x<=100.

x has successor x+1 if x is a number.

x has reachable y if
    x has successor y or
    x has reachable z and z has reachable y.

query "Reachable" has Reachable n if 95 has reachable n.

