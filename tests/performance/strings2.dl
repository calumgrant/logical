expected-results 1.
evaluation-step-limit 69960014.
memory-limit-mb 2000.

S is string if 1<=N<=10000 and 1<=M<1000 and S = N + "-" + M.

C is Count if 
    C = find count S in (string S).

select c if Count c.
