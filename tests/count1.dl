f X if X = count Y in (1<=Y<=10 or 11<Y<=20 or Y=15).

error "count" if not f 19.

X has fewer Y if 1<=X<=10 and Y = count Z in (0<Z<X).

find _ has fewer _.

error "count 2" if not 2 = count X in (X=1 or X=2).

prime N if 2<=N<1000 and not (2<=M<N and N%M=0).

error "Primes" if not 168 = count N in (prime N).

// find count N if prime N.

// Can do weird stuff like count X/2 in ().

